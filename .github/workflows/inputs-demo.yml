name: Inputs Demo
on:
  workflow_dispatch:
    inputs:
      environmet:
        description: Chose the env to deploy
        required: true
        default: dev
jobs:
  inputs_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo "Deploy the application to ${{inputs.environmet}}"
