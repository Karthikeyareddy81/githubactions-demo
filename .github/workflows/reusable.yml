name: Reusable Workflow
on:
    workflow_call:
        inputs:
            name:
                description: Enter the input name
                required: true
                type: string

jobs:
    greetings: 
        runs-on: ubuntu-latest
        steps:
            - name: Greeting step
              run: echo "Hi ${{inputs.name}}, Welcome to the reusable workflow!"
                