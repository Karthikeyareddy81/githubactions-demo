name: Reusable Workflow
on:
    workflow_call:
jobs:
    greetings: 
        runs-on: ubuntu-latest
        steps:
            - name: Greeting step
              run: echo "Welcome to the reusable workflow!"
                