name: ENV Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env_demo:
    runs-on: ubuntu-latest
    steps:
      - name: conneting to env
        run: echo "App is connecting to $APP_ENV"
        
