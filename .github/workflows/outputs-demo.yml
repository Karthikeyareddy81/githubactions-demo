name: Outputs demo
on: workflow_dispatch
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        id: step1
        run: echo "version=v1.1.0" >> $GITHUB_OUTPUT
      - name: step 2
        run: echo "This version is ${{steps.step1.outputs.version}}"
        
